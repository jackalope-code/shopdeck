# Postgres credentials — must match the values in backend/.env
# docker-compose.yml reads these via ${POSTGRES_PASSWORD} etc.
POSTGRES_DB=shopdeck
POSTGRES_USER=shopdeck
POSTGRES_PASSWORD=shopdeck_dev